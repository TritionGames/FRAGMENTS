import os

import pygame as pg
pg.init()

font = pg.font.Font(os.path.join(os.getcwd(), 'assets', 'font', 'font.ttf'))

font_40 = pg.font.Font(os.path.join(os.getcwd(), 'assets', 'font', 'font.ttf'), 40)